import { DefaultIspImageDirective } from './default-isp-image.directive';

describe('DefaultIspImageDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultIspImageDirective();
    expect(directive).toBeTruthy();
  });
});
